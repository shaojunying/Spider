# Spider Toolkit 环境配置文件示例
# 复制此文件为 .env 并填入实际值

# HTTP 请求配置
HTTP_TIMEOUT=30
HTTP_RETRIES=3
HTTP_DELAY_MIN=1.0
HTTP_DELAY_MAX=3.0
USER_AGENT="Spider-Toolkit/1.0"

# 数据库配置 (可选)
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=your_username
DB_PASSWORD=your_password
DB_DATABASE=your_database

# 安全配置
SSL_VERIFY=true
MAX_FILE_SIZE=10485760  # 10MB

# 日志配置
LOG_LEVEL=INFO
LOG_FILE_ENABLED=true
LOG_CONSOLE_ENABLED=true

# 输出目录配置
OUTPUT_DIR=output
LOGS_DIR=logs

# API 密钥 (根据需要添加)
# OPENAI_API_KEY=your_openai_api_key
# GITHUB_TOKEN=your_github_token

# 爬虫特定配置
# ANJUKE_COOKIE=your_anjuke_cookie
# HOSPITAL_USERNAME=your_hospital_username
# HOSPITAL_PASSWORD=your_hospital_password

# 邮件配置 (用于通知)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your_email@gmail.com
# SMTP_PASSWORD=your_email_password

# 代理配置 (可选)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080